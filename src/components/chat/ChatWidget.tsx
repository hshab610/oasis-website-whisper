import { Suspense, lazy } from 'react';

// Original ChatWidget implementation without promotion context
const ChatWidget = () => {
  // No promotion context is needed anymore
  return null;
};

export default ChatWidget;
