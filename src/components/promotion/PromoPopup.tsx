
// This component has been kept as a no-op placeholder
// since it might be referenced from other files
const PromoPopup: React.FC<{ trigger?: 'timer' | 'exit' }> = () => {
  // Component is now a no-op
  return null;
};

export default PromoPopup;
